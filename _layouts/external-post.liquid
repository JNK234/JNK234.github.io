---
layout: post
---

<!-- ABOUTME: Layout template for displaying external blog posts from Medium/Substack -->
<!-- ABOUTME: Redirects to external URL while showing preview on the blog listing -->

<div class="external-post-notice">
  <p><strong>Note:</strong> This post was originally published on {{ page.external_source }}.</p>
  <a href="{{ page.redirect }}" target="_blank" rel="external nofollow noopener" class="btn btn-primary">
    Read Full Article on {{ page.external_source }} →
  </a>
</div>

<div class="post-content">
  {% if page.feed_content %}
    {{ page.feed_content | truncatewords: 100 }}
  {% elsif page.description %}
    {{ page.description }}
  {% else %}
    {{ content }}
  {% endif %}
</div>

<script>
  // Optional: Auto-redirect after a few seconds
  // setTimeout(function() {
  //   window.location.href = "{{ page.redirect }}";
  // }, 3000);
</script>